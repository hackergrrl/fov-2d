{
  "name": "fov-2d",
  "description": "check whether a point is in an observer's field-of-view",
  "version": "1.0.0",
  "repository": {
    "url": "git://github.com/noffle/fov-2d.git"
  },
  "main": "index.js",
  "scripts": {
    "test": "tape test/*.js"
  },
  "dependencies": {
    "gl-vec2": "^1.0.0"
  },
  "devDependencies": {
    "tape": "*"
  }
}
